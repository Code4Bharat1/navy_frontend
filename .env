NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api

# No Razorpay key needed here — the backend hands the frontend a public key id
# per recharge session (see /api/recharge/initiate), and the checkout script
# (https://checkout.razorpay.com/v1/checkout.js) is the same for every merchant.
